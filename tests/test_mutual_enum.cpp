#include <gtest/gtest.h>
#include <mutual_enum/mutual_enum.hpp>

TEST(mutual_enum_test, Basic)
{
	// check for sizeof enums and so on...
	EXPECT_EQ(1, 1);
}

TEST(mutual_enum_test, EdgeCase)
{
	// tests go here...
	EXPECT_EQ(0, 1);
}
